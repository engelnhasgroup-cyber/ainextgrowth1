'use client'

import { Clock, ArrowRight, Sparkles } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import type { ItemSummary } from '@/lib/types'
import { ItemCard } from './item-card'
import { useLibrary } from './store'

function timeAgo(dateStr: string): string {
  const d = new Date(dateStr)
  const diff = Date.now() - d.getTime()
  const mins = Math.floor(diff / 60000)
  if (mins < 1) return 'just now'
  if (mins < 60) return `${mins}m ago`
  const hrs = Math.floor(mins / 60)
  if (hrs < 24) return `${hrs}h ago`
  const days = Math.floor(hrs / 24)
  if (days < 7) return `${days}d ago`
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })
}

export function RecentSection({ items }: { items: ItemSummary[] }) {
  const setSort = useLibrary((s) => s.setSort)
  if (items.length === 0) return null

  const top = items.slice(0, 4)

  return (
    <section id="recent" className="scroll-mt-20 border-b border-border/60 py-12 sm:py-16">
      <div className="mx-auto max-w-7xl px-4 sm:px-6">
        <div className="mb-7 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
          <div>
            <div className="mb-2 inline-flex items-center gap-2 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-3 py-1 text-xs font-semibold text-emerald-400">
              <Clock className="h-3.5 w-3.5" />
              Recently Added · Fresh from the Agent
            </div>
            <h2 className="text-2xl font-bold tracking-tight sm:text-3xl">
              Newest Prompts, Skills & Workflows
            </h2>
            <p className="mt-1.5 max-w-2xl text-sm text-muted-foreground">
              The latest items generated by our autonomous AI agent. Each includes a full Trinity
              Bundle and a substantive SEO introduction reviewed by our editorial team.
            </p>
          </div>
          <Button
            variant="outline"
            size="sm"
            className="shrink-0 rounded-full"
            onClick={() => {
              setSort('newest')
              setTimeout(() => {
                document.getElementById('library')?.scrollIntoView({ behavior: 'smooth' })
              }, 50)
            }}
          >
            View All Newest
            <ArrowRight className="ml-1.5 h-3.5 w-3.5" />
          </Button>
        </div>

        <div className="grid grid-cols-1 gap-3.5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
          {top.map((item, i) => (
            <div key={item.id} className="relative">
              <ItemCard item={item} index={i} />
              <span className="absolute -left-1 -top-2 z-20 inline-flex items-center gap-1 rounded-full bg-emerald-500 px-2 py-0.5 text-[9px] font-bold text-white shadow-lg">
                <Sparkles className="h-2.5 w-2.5" />
                {timeAgo(item.createdAt)}
              </span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
